subscription RoomSubscription($id: ID!) {
  gameCycle(roomId: $id) {
    ... on DeltaEvent {
      index
      type
      username
      word
    }
    ... on EndEvent {
      type
    }
    ... on StartEvent {
      type
    }
  }
}
